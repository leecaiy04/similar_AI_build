# 备注功能使用说明

## 功能概述

为相似度对比的每个锁定匹配项添加备注功能，方便记录对比过程中的思考、判断依据或特殊说明。

## 功能特点

### 📝 核心功能
- **备注输入**：为每个锁定的匹配项添加自定义备注
- **实时保存**：备注内容自动保存到本地存储
- **持久化**：刷新页面后备注依然保留
- **导出包含**：所有导出功能（CSV、Excel、剪贴板）都包含备注列

### 🎯 使用场景

#### 场景 1：记录匹配依据
```
源项：浙江阿里巴巴云计算有限公司
匹配：阿里巴巴云计算
备注：已确认为同一实体，地域信息已核实
```

#### 场景 2：标记特殊情况
```
源项：北京字节跳动科技有限公司
匹配：字节跳动
备注：2024年3月更名，原名今日头条科技
```

#### 场景 3：记录待确认事项
```
源项：深圳市腾讯计算机系统有限公司
匹配：腾讯
备注：需向法务部确认主体是否一致
```

#### 场景 4：批量处理标记
```
源项：小米通讯技术有限公司
匹配：小米科技
备注：批次#202406-001，已通过人工审核
```

## 使用方法

### 1. 添加备注

1. 完成相似度对比
2. 点击"锁定建议"锁定一个匹配项
3. 在锁定区域会出现"📝 备注"输入框
4. 输入备注内容（支持多行）
5. 备注自动保存

### 2. 查看备注

- 锁定的匹配项会在"已确认匹配"区域显示备注输入框
- 备注内容在输入框中可见
- 可随时修改备注内容

### 3. 编辑备注

1. 找到已锁定的匹配项
2. 在备注输入框中直接修改
3. 修改自动保存

### 4. 导出包含备注

**方式一：复制到剪贴板**
```
源项	锁定匹配	锁定相似度	备注	第1相似值	第1相似度	...
浙江阿里巴巴云计算有限公司	阿里巴巴云计算	95.50%	已确认为同一实体	阿里巴巴云计算	95.50%	...
```

**方式二：导出锁定项（CSV）**
```csv
模式,基准文本(视图),锁定配对,相似度,状态,备注
left,浙江阿里巴巴云计算有限公司,阿里巴巴云计算,95.50%,已锁定,已确认为同一实体
```

**方式三：全局报告（CSV）**
```csv
源项,锁定匹配,锁定相似度,备注,第1相似值,第1相似度,...
浙江阿里巴巴云计算有限公司,阿里巴巴云计算,95.50%,已确认为同一实体,阿里巴巴云计算,95.50%,...
```

## UI 设计

### 备注输入框样式

**常规状态**：
- 淡绿色边框
- 半透明白色背景
- 圆角设计

**聚焦状态**：
- 高亮绿色边框
- 纯白色背景
- 绿色阴影

**暗色模式**：
- 深绿色背景
- 青绿色文字
- 自动适配暗色主题

### 位置布局

```
┌─────────────────────────────────────┐
│ 已确认匹配           [解除]          │
├─────────────────────────────────────┤
│ 阿里巴巴云计算  ✓ 95.5%             │
├─────────────────────────────────────┤
│ 📝 备注                              │
│ ┌─────────────────────────────────┐ │
│ │ 添加备注信息...                 │ │
│ │                                 │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

## 技术实现

### 数据结构

```typescript
interface LockedItem {
  matchIndex: number
  text: string
  similarity: number
  note?: string  // 新增备注字段
}
```

### 核心函数

```typescript
// 更新备注
updateNote(item: BatchResult, note: string): void

// 获取备注
getNote(item: BatchResult): string
```

### 存储机制

- 备注随锁定项存储在 `lockedItems` Map 中
- 自动保存到 localStorage
- 支持工作区导出/导入

## 最佳实践

### 备注规范建议

**简洁明了**
```
✅ 已确认为同一实体
❌ 经过仔细比对和多方确认，最终判定这两个名称指向的是同一个法律实体
```

**结构化标记**
```
✅ [待确认] 需联系财务部
✅ [已审核] 2024-06-08 张三
✅ [批次] 202406-001
```

**关键信息优先**
```
✅ 更名：原深圳华为技术
✅ 合并：由A公司和B公司合并
✅ 风险：存在商标争议
```

### 工作流程建议

**第一轮：快速筛选**
1. 启动对比
2. 锁定高相似度项
3. 暂不添加备注

**第二轮：人工审核**
1. 筛选已锁定项
2. 逐个审核
3. 添加备注说明

**第三轮：导出交付**
1. 复制到剪贴板
2. 粘贴到 Excel
3. 备注列用于后续追溯

## 常见问题

### Q: 备注会丢失吗？
A: 不会。备注自动保存到浏览器本地存储，除非清除浏览器数据或主动重置缓存。

### Q: 备注有字数限制吗？
A: 没有硬性限制，但建议控制在 200 字以内，保持简洁。

### Q: 可以批量添加备注吗？
A: 目前需要逐个添加。可以先在 Excel 中批量编辑，然后通过"导入锁定"功能导入。

### Q: 未锁定的项可以添加备注吗？
A: 不可以。必须先锁定匹配项，才能添加备注。这是为了确保备注与确认的匹配关系绑定。

### Q: 备注可以导出到 Excel 吗？
A: 可以。所有导出功能（CSV、复制到剪贴板）都包含备注列。

### Q: 导入带备注的 CSV 文件需要特定格式吗？
A: 需要。简单导出格式的第 6 列为备注，全局报告格式的第 4 列为备注。保持列顺序不变即可正确导入。

## 更新日志

### v1.2.0 (2026-06-08)
- ✨ 新增备注功能
- 📝 支持多行文本输入
- 💾 自动持久化保存
- 📊 导出功能包含备注列
- 📥 导入功能支持备注

## 相关功能

- [隐藏低于阈值项](./FEATURE_UPDATE.md#1-隐藏低于阈值的匹配项-)
- [复制到剪贴板](./FEATURE_UPDATE.md#2-复制到剪贴板功能-)
- [锁定匹配项](./README.md#锁定功能)
- [导出功能](./README.md#导出功能)

## 反馈与建议

如有问题或建议，请访问：
- 📧 提交 Issue
- 💬 联系开发团队
- 📚 查看完整文档
