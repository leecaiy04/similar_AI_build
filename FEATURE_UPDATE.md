# 相似度功能更新说明

## 新增功能

### 1. 隐藏低于阈值的匹配项 🚫

**功能描述**：
- 在筛选栏中新增"隐藏低于阈值"开关（🚫 图标）
- 启用后，将自动隐藏所有没有匹配项且未被锁定的对比结果
- 只显示有匹配结果或已锁定的项

**使用场景**：
- 大数据量对比时，快速聚焦有效匹配
- 过滤掉无法匹配的噪音数据
- 提高结果查看效率

**使用方法**：
1. 完成相似度对比后
2. 点击搜索框右侧的 🚫 图标（橙色高亮表示已启用）
3. 页面自动隐藏低于阈值的项
4. 再次点击可恢复显示全部结果

**技术实现**：
- 在 `filterOptions` 中新增 `hideSubThreshold` 字段
- 在 `similarityService.ts` 的 `buildDisplayResults` 方法中添加过滤逻辑
- 过滤条件：`!hasMatches && !hasLocked`

---

### 2. 复制到剪贴板功能 📋

**功能描述**：
- 新增"📋 复制到剪贴板"按钮
- 一键将对比结果复制为 TSV 格式（Tab 分隔值）
- 可直接粘贴到 Excel 或 Google Sheets

**使用场景**：
- 快速将结果分享给其他人
- 在 Excel 中进行二次加工
- 不想下载文件，只需临时复制数据

**使用方法**：
1. 完成相似度对比后
2. 点击"📋 复制到剪贴板"按钮
3. 系统提示"已复制 X 行数据到剪贴板"
4. 打开 Excel 或任意表格软件
5. 按 `Ctrl+V`（或 `Cmd+V`）粘贴
6. 数据自动对齐到表格列

**导出格式**：
```
源项	锁定匹配	锁定相似度	第1相似值	第1相似度	第2相似值	第2相似度	...
浙江阿里巴巴云计算有限公司	阿里巴巴云计算	95.50%	阿里巴巴云计算	95.50%	阿里云	85.20%	...
```

**技术实现**：
- 使用 `navigator.clipboard.writeText()` API
- 数据格式为 TSV（Tab-Separated Values）
- 包含表头和最多 5 个匹配项

**浏览器兼容性**：
- Chrome 63+
- Firefox 53+
- Safari 13.1+
- Edge 79+

---

## UI 变化

### 筛选栏更新

**之前**：
```
[搜索框] [.*]  [锁定状态] [匹配状态] [重置]
```

**现在**：
```
[搜索框] [.*] [🚫]  [锁定状态] [匹配状态] [重置]
```

### 导出按钮区域更新

**之前**：
```
[导入锁定] [导出锁定] [全局报告]
```

**现在**：
```
[导入锁定] [导出锁定] [📋 复制到剪贴板] [全局报告]
```

---

## 快捷操作组合

### 组合 1：快速筛选有效匹配
1. 启用"隐藏低于阈值"（🚫）
2. 选择"匹配状态 → 已匹配"
3. 只显示成功匹配的项

### 组合 2：检查未匹配项
1. 禁用"隐藏低于阈值"
2. 选择"匹配状态 → 未匹配"
3. 查看所有无法匹配的源项

### 组合 3：导出已确认的匹配
1. 锁定需要导出的匹配项
2. 选择"锁定状态 → 已锁定"
3. 点击"📋 复制到剪贴板"
4. 只复制已确认的匹配关系

---

## 技术细节

### 文件修改清单

1. **useSimilarityWorkspace.ts**
   - 新增 `filterOptions.hideSubThreshold` 字段
   - 新增 `copyToClipboard()` 方法
   - 导出 `copyToClipboard` 函数

2. **similarityService.ts**
   - 更新 `SimilarityFilterOptions` 接口，添加 `hideSubThreshold` 字段
   - 更新 `buildDisplayResults()` 方法，添加过滤逻辑

3. **SimilarityPage.vue**
   - 筛选栏添加"隐藏低于阈值"按钮
   - 导出区域添加"复制到剪贴板"按钮
   - 导入 `copyToClipboard` 函数

4. **similarityService.spec.ts**
   - 更新测试用例，补充 `hideSubThreshold: false`

### 类型定义

```typescript
interface SimilarityFilterOptions {
  lockStatus: 'all' | 'locked' | 'unlocked'
  matchStatus: 'all' | 'matched' | 'unmatched'
  searchQuery: string
  isRegexSearch: boolean
  hideSubThreshold: boolean  // 新增
}
```

---

## 测试清单

### 功能测试

- [x] 隐藏低于阈值开关正常工作
- [x] 复制到剪贴板功能正常
- [x] TSV 格式可正确粘贴到 Excel
- [x] 筛选条件组合正常
- [x] 构建成功无报错

### 边界测试

- [ ] 无结果时点击复制按钮 → 应提示"暂无结果可复制"
- [ ] 复制后 Excel 列对齐正确
- [ ] 隐藏开关与其他筛选条件配合正常
- [ ] 重置筛选时恢复 `hideSubThreshold: false`

### 浏览器测试

- [ ] Chrome
- [ ] Firefox
- [ ] Safari
- [ ] Edge

---

## 使用示例

### 场景 1：清理企业名称库

**目标**：从 1000 个原始企业名称中，找出能匹配标准库的项

**步骤**：
1. 输入源列表（1000 个原始名称）
2. 输入目标库（500 个标准名称）
3. 设置阈值 70%
4. 启动比对
5. 启用"隐藏低于阈值"
6. 选择"匹配状态 → 已匹配"
7. 复制结果到剪贴板
8. 粘贴到 Excel 进行下一步处理

**结果**：只看到和复制了能匹配的 600+ 项，节省大量筛选时间

---

### 场景 2：检查遗漏项

**目标**：找出哪些源项无法匹配到标准库

**步骤**：
1. 完成比对后
2. 禁用"隐藏低于阈值"（确保显示所有项）
3. 选择"匹配状态 → 未匹配"
4. 查看无法匹配的源项
5. 复制到剪贴板
6. 手动审核这些遗漏项

---

## 未来优化方向

- [ ] 支持复制为 CSV 格式
- [ ] 支持自定义导出列
- [ ] 添加复制成功的视觉反馈动画
- [ ] 支持复制选中的部分结果
- [ ] 阈值滑块旁边显示"隐藏低于阈值"的动态数量提示

---

## 版本信息

- **更新日期**：2026-06-08
- **版本**：v1.1.0
- **更新内容**：新增隐藏低阈值项和复制到剪贴板功能
